<h1>Edit Topic</h1>
<hr>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for topics:</p>
    <ul>
      <li>Choose a topic for grouping your urls</li>
      <li>Make it easy to remember</li>
      <li>Keep it short but conscise</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= form_for @topic do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter topic name" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
      <%= link_to "Cancel", :back, {:class=>"btn btn-default"} %>

      <% end %>
  </div>
</div>
