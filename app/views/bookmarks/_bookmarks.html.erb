<br>
Bookmarks</br>
<small>
  <%= "It's lonely with no bookmarks! :)" if topic.bookmarks.blank? %>
  <% topic.bookmarks.each do |the_bookmark| %>
   <%# Why do I have to call out each param %>
   <%= link_to "", topic_bookmark_path(topic_id: the_bookmark.topic_id, id: the_bookmark.id) , method: :delete, class: 'glyphicon glyphicon-remove' %>
   <%= link_to "", edit_topic_bookmark_path(topic_id: the_bookmark.topic_id, id: the_bookmark.id), method: :get,  class: 'glyphicon glyphicon-edit' %>
   <%= link_to "#{the_bookmark.url}", "#{the_bookmark.url}", target: "_blank" %>
    <br>
  <% end %>
</small>
